language: node_js

node_js:
    - "node"
    - "4"

os:
    - linux

env:
    - NODE_ENV=TEST

install:
    - npm install

script:
    - pwd
    - whoami
    - ls -ltr ./node_modules/.bin
    - npm run print-banner
    - npm test

notifications:
    email: "smallela.projworks@gmail.com"

